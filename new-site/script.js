document.querySelectorAll('.character video').forEach(video=>{const box=video.parentElement;const move=x=>{const r=box.getBoundingClientRect();const p=Math.max(0,Math.min(1,(x-r.left)/r.width));if(video.duration)video.currentTime=p*video.duration};box.addEventListener('pointermove',e=>move(e.clientX));box.addEventListener('touchmove',e=>e.touches[0]&&move(e.touches[0].clientX),{passive:true});box.addEventListener('pointerleave',()=>{if(video.duration)video.currentTime=video.duration/2})});
